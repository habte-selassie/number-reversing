const string  = 'hey';
const arraystring = string.slice("",9); ///// first convert the string to array of strings.

let combinator = ['h','e','y','hey','he','hy','ey','yhe','yh','ye','ehy','eh','eyh'];/////// listing the combination of the word hey 
let leng = combinator.length;//////////// calculating the length of our combinator array.
   console.log(leng);


let randomification = Math.trunc(Math.random()*12); ///////// produces random number range from 0-12.
console.log(randomification);

function stringGenerator(combinator) {
    
   for (let i = 0; i < combinator.length; i++) { /////////// looping over our combinator array.

      }
   } 
console.log(combinator[randomification]);////////// using the result of randomifcation to produce random word or string from our combinator array

stringGenerator(arraystring,combinator); 































